{
  "name": "phaser-parcel",
  "version": "1.0.0",
  "description": "Phaser project with parcel-bundler",
  "main": "index.js",
  "scripts": {
    "build": "parcel build src/index.html --public-url './'",
    "watch": "parcel watch src/index.html",
    "start": "parcel serve src/index.html",
    "clean": "rm -v dist/*",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "phaser",
    "phaser3"
  ],
  "author": "samme",
  "license": "ISC",
  "dependencies": {
    "colors.css": "^3.0.0",
    "phaser": "^3.15.1"
  },
  "devDependencies": {}
}
